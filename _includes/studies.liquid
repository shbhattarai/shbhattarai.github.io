<div class="col">
  <a href="{% if studies.redirect %}{{ studies.redirect }}{% else %}{{ studies.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if studies.img %}
        {%
          include figure.liquid
          loading="eager"
          path=studies.img
          sizes = "250px"
          alt="studies thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ studies.title }}</h2>
        <p class="card-text">{{ studies.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if studies.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ studies.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if studies.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ studies.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
